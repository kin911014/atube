header.header
    .header__column
        a(href=routes.home)
            <i class="fab fa-youtube"></i>
    .header__column
        form(action=routes.search, method="get")
            input(type="text", placeholder="Searching for term...", name="term")
    
    .header__column
        ul
            li
                a(href=routes.join) Join
            li
                a(href=routes.login) Log In
            